{% extends 'base.html.twig' %}

{% block body %}
    <div class="back-office">
        <h1>Détails d'un Joueur</h1>
        <a href="{{ path('app_back_player_index') }}" class="btn btn-warning">retour au menu</a>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ player.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ player.name }}</td>
                </tr>
                <tr>
                    <th>FirstName</th>
                    <td>{{ player.firstName }}</td>
                </tr>
                <tr>
                    <th>NickName</th>
                    <td>{{ player.nickName }}</td>
                </tr>
                <tr>
                    <th>JerseyNumber</th>
                    <td>{{ player.jerseyNumber }}</td>
                </tr>
                <tr>
                    <th>StickSide</th>
                    <td>{{ player.stickSide }}</td>
                </tr>
                <tr>
                    <th>Picture</th>
                    <td>{{ player.picture }}</td>
                </tr>
                <tr>
                    <th>Role</th>
                    <td>{{ player.role }}</td>
                </tr>
            </tbody>
        </table>

        

        <a href="{{ path('app_back_player_edit', {'id': player.id}) }}" class="btn btn-warning">modifier</a>

        {{ include('back/player/_delete_form.html.twig') }}
    </div>
{% endblock %}
